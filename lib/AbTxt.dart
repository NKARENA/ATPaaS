import 'package:flutter/material.dart';

class AbTxt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "ATPaaS",
      style: TextStyle(
        fontSize: 40,
        fontWeight: FontWeight.w400,
      ),
    );
  }
}
